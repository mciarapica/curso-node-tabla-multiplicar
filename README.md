Info para ejecutar el programa

```
Opciones:
      --help     Muestra ayuda                                        [booleano]
      --version  Muestra número de versión                            [booleano]
  -b, --base     Es la base de la tabla de multiplicar     [número] [defecto: 1]
  -l, --listar   Indica si se imprime la tabla de multiplicar en pantalla
                                                     [booleano] [defecto: false]
  -h, --hasta    Indica hasta que número se va a multplicar la base
                                                          [número] [defecto: 10]
```